{
  "name": "medic-upgrade-service",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node src/main/index.js",
    "test": "./src/utils/init && jest || true && ./src/utils/destroy || true",

    "test-on-pre-existing-cluster": "./src/utils/init-assume-cluster-exists && jest",
    "coverage": "./src/utils/init && jest --coverage || true && ./src/utils/destroy || true",
    "compile": "tsc",
    "build-docker-image": "docker build . -t medicmobile/upgrade-service:0.22"
  },
  "author": "Henok",
  "license": "ISC",
  "dependencies": {
    "@kubernetes/client-node": "^0.16.1",
    "@types/node": "^17.0.8",
    "cors": "^2.8.5",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "@types/express": "^4.17.13",
    "@types/jest": "^27.4.0",

    "jest": "^27.4.7",
    "ts-jest": "^27.1.2",
    "typescript": "^4.5.3"
  }
}
